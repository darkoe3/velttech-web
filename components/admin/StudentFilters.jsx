"use client";
import { useMemo, useState } from "react";
export default function StudentFilters({students, renderStudent}){const[search,setSearch]=useState("");const[status,setStatus]=useState("");const[school,setSchool]=useState("");const filtered=useMemo(()=>students.filter(s=>(!search||`${s.first_name} ${s.other_name} ${s.last_name}`.toLowerCase().includes(search.toLowerCase()))&&(!status||s.approval_status===status)&&(!school||s.school_name?.toLowerCase().includes(school.toLowerCase()))),[students,search,status,school]);return <><div className="mb-5 grid gap-3 md:grid-cols-3"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search by name" className="rounded-xl border px-4 py-3"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border px-4 py-3"><option value="">All statuses</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select><input value={school} onChange={e=>setSchool(e.target.value)} placeholder="Filter by school" className="rounded-xl border px-4 py-3"/></div>{filtered.length===0?<p className="rounded-xl border bg-white p-6 text-slate-600">No students match the current filters.</p>:<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(renderStudent)}</div>}</>}
