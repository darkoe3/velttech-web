"use client";
import { useMemo, useState } from "react";
export default function EnrollmentFilters({enrollments,renderEnrollment}){const[search,setSearch]=useState("");const[status,setStatus]=useState("");const filtered=useMemo(()=>enrollments.filter(e=>(!search||`${e.student_detail?.first_name} ${e.student_detail?.last_name} ${e.course_detail?.title}`.toLowerCase().includes(search.toLowerCase()))&&(!status||e.status===status)),[enrollments,search,status]);return <><div className="mb-5 grid gap-3 md:grid-cols-2"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search student/course" className="rounded-xl border px-4 py-3"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border px-4 py-3"><option value="">All statuses</option><option value="pending">Pending</option><option value="active">Active</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select></div>{filtered.length===0?<p className="rounded-xl border bg-white p-6 text-slate-600">No enrollments match the current filters.</p>:<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(renderEnrollment)}</div>}</>}
